### useDispatch()

**Definition:**

useDispatch() is a React-Redux hook that returns the store's dispatch function, allowing components to dispatch actions.

Example:
```javascript
const dispatch = useDispatch();

dispatch(addItem(product));
```
