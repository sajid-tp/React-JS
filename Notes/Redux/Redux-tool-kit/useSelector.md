### Selector

**Definition:**

useSelector() is a React-Redux hook that allows a component to subscribe to and read data from the Redux store.
```javascript
const cart = useSelector(state => state.cart);
```
