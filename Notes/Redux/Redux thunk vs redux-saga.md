### Redux-Saga — Definition

Redux-Saga is a middleware library for Redux that handles complex asynchronous operations and side effects, such as API calls, delays, and background tasks, by using generator functions

### Redux thunk
Redux Thunk is a Redux middleware that allows you to dispatch functions (thunks) instead of only plain action objects, making it possible to handle asynchronous operations such as API calls.
